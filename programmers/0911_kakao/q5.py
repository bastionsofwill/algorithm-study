class Node:
    def __init__(self, is_sheep, c1, c2):
        self.is_sheep = is_sheep
        self.c1 = c1
        self.c2 = c2
    
def solution(info, edges):
    bin_tree = []
    edge_list = [[0 for x in range(info)] for y in range(info)]
    for 
    for i in range(len(info)):
        for j in edge_list[i]:
            if j ==
        bin_tree.append(Node(info[i],)
        

    answer = 0
    return answer